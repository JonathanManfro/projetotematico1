package projetotematico;

import javax.swing.*;
import java.awt.*;

public class ResultScreen extends JFrame {
    public ResultScreen() {
        setTitle("Tela de Resultado");
        setSize(800, 600);
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        setLocationRelativeTo(null);

        // Adicione os componentes e o conteúdo da tela de resultado aqui

        setVisible(true);
    }
}
